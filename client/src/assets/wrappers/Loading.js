import styled from 'styled-components';

const Wrapper = styled.div`
  .skeleton {
    background: #ccc;
    border-radius: 4px;
    width: 100%;
    height: 100%;
  }

  .skeleton-header,
  .skeleton-body {
    background: #eee;
    height: 20px;
    margin: 10px 0;
  }
`;
export default Wrapper;
